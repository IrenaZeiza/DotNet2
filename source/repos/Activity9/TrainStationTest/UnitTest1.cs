using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace TrainStationTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void AnnounceArrival()
        {
            //6) Test class TrainStation class with at least two separate event handler
            //methods(i.e.subscribers).
            //7) To test the event, you should declare an object of type TrainStation,
            //instantiate it, subscribe to the Arrival event and activate this event by
            //calling method AnnounceArrival(). The class must declare an event
            //handler method, e.g., void ArrivalHandler(object sender,
            //ArrivalEventArgs args) that responds to these events.
        }
      // [TestMethod]
        // void ArrivalHandler(object sender, ArrivalEventArgs args)
        //{

        //}
    } }

     
